[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "stackly-pos"
version = "0.1.0"
description = "Stackly POS Flask application"
requires-python = ">=3.10"
dependencies = []
